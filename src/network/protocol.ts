/**
 * NOTE(@rayhanadev): we now use protobufs to define the protocol
 * @see {@link src/network/proto/chatter/v1/protocol.proto}
 */

export const ALPN = "rayhanadev/chatter/1.0";
export const MAX_MESSAGE_SIZE = 64 * 1024;
